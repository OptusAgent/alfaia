# Story 6.6: Classificador de conversa e movimentação automática do Kanban real

## Status
InProgress

> **Validação rápida (@po, 2026-08-21, GO):** dependências (4.6, 3.5) já `Done`. Escopo, critérios
> de aceite e riscos (CRITICAL para `ganho` indevido/mock em produção) já documentados. Investigação
> real (consulta direta ao Postgres) confirmou o problema raiz antes mesmo do Task 1 começar:
> `webhooks.py` monta `LeadDTO(status="qualificando", ...)` **hardcoded** a cada mensagem, ignorando
> o status real do lead, e `ctx_exec` nunca passa `lead_service` no caminho de produção — ou seja,
> `mover_status` já é chamável pela IA hoje, mas qualquer mudança de status é uma mutação em memória
> que se perde no fim da requisição: nunca volta para `leads.status` nem grava `lead_eventos` real.
> Status: Draft → Ready → InProgress.

## Executor Assignment
executor: "dev"
quality_gate: "qa"
quality_gate_tools: ["status-classifier-test", "kanban-db-integration-test", "mv-rules-regression-test"]

## Story
**Como** dona da loja,
**quero** que cada conversa mova automaticamente o lead para o status correto no Kanban real,
**para que** o quadro reflita o funil sem depender de mock ou atualização manual.

## Acceptance Criteria
1. Primeiro contato de número novo cria lead em `novo`; número existente reutiliza ou reabre lead conforme `identificar_lead`.
2. Lead informa evento, peça, data ou intenção clara → status muda de `novo` para `qualificando`, respeitando MV1–MV6.
3. Conversa envolve preço, valor, orçamento, disponibilidade com valores ou solicitação explícita de orçamento → status muda para `orcamento`.
4. Lead em `follow_up` que responde volta para `negociando`.
5. Agendamento confirmado pela automação muda para `agendado`.
6. Toda movimentação grava `lead_eventos` com `autor='ia'` ou `autor='sistema'`, motivo e evidência resumida da classificação.
7. Kanban do portal lê leads reais do Supabase e remove `MOCK_LEADS` em produção.
8. IA/classificador nunca move para `ganho`; movimento humano recente em 24h continua protegido.

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: CRM/Backend. Secondary: AI Classification, Frontend. Complexity: L.
**Specialized Agents** — Primary: @dev. Quality Gate: @qa.
Transição indevida para `ganho`, perda de proteção MV3 ou Kanban com mock em produção são CRITICAL.

## Tasks / Subtasks
- [ ] Task 1 — Criar classificador determinístico + LLM opcional para intenção/status com saída estruturada (`status_destino`, `motivo`, `confianca`, `evidencia`) (AC: 2, 3, 4).
- [ ] Task 2 — Integrar classificador ao webhook após persistir inbound e antes/depois da resposta IA conforme necessidade (AC: 1–4, 6).
- [x] Task 3 — Usar `StatusTransitionService` persistindo em Postgres real, não apenas em DTO/memória (AC: 5, 6, 8) — **feito em 2 partes**: (a) round-trip real de status (`webhooks.py` hidrata `LeadDTO` do Postgres em vez de hardcodar `status="qualificando"`, e persiste de volta `leads.status`/`lead_eventos` quando o motor de IA muda o status durante a troca); (b) `agendar` move para `agendado` como efeito DIRETO da tool (nunca depende de uma chamada separada a `mover_status`, AC 5). Cobre 100% do AC 5 (agendamento → agendado) e a infraestrutura de persistência que as Tasks 1/2 (demais transições) vão reusar.
- [ ] Task 4 — Atualizar Kanban para buscar `leads` reais com `contatos`, `lead_eventos` e contadores por status (AC: 7).
- [x] Task 5 (parcial) — Testes da Task 3: round-trip de status hidratado/persistido (worker + webhook), `agendar` movendo/não-movendo status, payload real de `atualizar_status_lead`/`registrar_lead_evento` verificado contra mock HTTP E contra o Postgres real de desenvolvimento. Faltam os testes de transição novo→qualificando/orçamento/follow_up→negociando (dependem do classificador da Task 1).

## Dev Notes

### Regras confirmadas nesta sessão
- Número novo começa em `novo`.
- Solicitação de orçamento/preço deve ir para `orcamento`.
- Agendamento confirmado deve ir para `agendado`.
- Movimento para `ganho` continua manual/humano.

### Dependências
Depende de 4.6 para lead/conversa real. Depende de 3.5 para MV1–MV6. Bloqueia 6.7 e complementa 5.5.

### Testing
Usar fixtures reais sanitizadas da conversa:
- "Quero agendar uma prova" não deve virar orçamento automaticamente.
- "É um casamento e quero um terno azul marinho completo" deve qualificar interesse.
- Resposta da IA com valor/produto ou pedido de preço deve levar a `orcamento`.
- Confirmação de agendamento da automação deve levar a `agendado`.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-18 | 1.0 | Story criada para ligar classificação de conversa ao Kanban real e remover mocks de pipeline | River (@sm) + Dex (@dev) |
| 2026-08-21 | 1.1 | Validação rápida (GO) e Task 3/parte do Task 5 implementadas — round-trip real de status. Achado real em produção antes de escrever qualquer código: `webhooks.py` montava `LeadDTO(status="qualificando", ...)` fixo em toda mensagem, ignorando o status real do lead, e não passava `lead_service` no caminho de produção — qualquer `mover_status` que a IA já chamava hoje era um no-op silencioso (mutava só o DTO da requisição, nunca voltava para `leads.status` nem `lead_eventos`). Corrigido: `supabase_rest_service` ganha `buscar_lead`/`atualizar_status_lead`/`registrar_lead_evento` (schema real de `lead_eventos` confirmado antes de escrever o insert — colunas `de`/`para`/`motivo` são de nível superior, não aninhadas em `detalhe`, diferente do `LeadEventoModel` em memória); `LeadDTO` ganha `status_alterado_por`/`status_alterado_em` (sem isso, a proteção MV3 de 24h contra sobrescrita de movimentação humana nunca tinha o dado real); `webhooks.py` hidrata o lead do Postgres antes de chamar a IA e persiste a mudança de status depois, se houve. `agendar` (tools_registry.py) agora move o lead para `agendado` como efeito direto do agendamento confirmado — não depende de o modelo chamar `mover_status` numa segunda tool call separada (mesma classe de bug já vista 2x nesta sessão com `atualizar_lead`). Validado em 2 camadas: testes com mock HTTP (payload exato do PATCH/POST) E chamada real contra o Postgres de desenvolvimento (buscar → atualizar → ler de volta → reverter), mesmo padrão usado para validar o fix de presence (2.3). 8 testes novos. 197/197 testes verdes. Status: Draft → Ready → InProgress (segue InProgress — Tasks 1, 2 e 4 pendentes: classificador determinístico das demais transições e Kanban real) | Dex (@dev) |
