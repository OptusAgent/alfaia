# Story 6.2: Timeline do lead (PRD: S-24)

## Status
Ready

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["timeline-completeness-test"]

## Story
**Como** atendente,
**quero** ver o histórico completo de um lead em ordem cronológica,
**para que** eu entenda o que já aconteceu antes de responder ou assumir a conversa.

## Acceptance Criteria
1. Toda alteração relevante aparece na timeline: criação, mudança de status (com autor), follow-up enviado, agendamento criado, transbordo aberto/fechado, interesse alterado, descarte, nota manual (§10.4).
2. Lead descartado, ao abrir a base, aparece com suas tags (AC 10.7).
3. Timeline ordenada por `criado_em desc`, usando o índice `lead_eventos_lead` (§17.4).

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Frontend. Secondary: Database. Complexity: S.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [ ] Task 1 — Componente de timeline lendo `lead_eventos` (AC: 1, 3)
- [ ] Task 2 — Mapear cada `tipo` de evento (`lead_criado`, `status_mudou`, `interesse_alterado`, `followup_enviado`, `transbordo_*`, `agendou`, `descartado`, `reaberto`, `divergencia_ia_humano`, `nota`) para uma representação visual (AC: 1)
- [ ] Task 3 — Exibir tags do contato junto ao lead na base (AC: 2)

## Dev Notes

### Schema (fonte literal: PRD §17.4)
```sql
create table lead_eventos (
  id bigserial primary key,
  tenant_id uuid not null references tenants(id) on delete cascade,
  lead_id uuid not null references leads(id) on delete cascade,
  tipo text not null,
  de text, para text,
  autor remetente not null default 'sistema',
  autor_user_id uuid references auth.users(id),
  motivo text,
  detalhe jsonb,
  criado_em timestamptz not null default now()
);
create index lead_eventos_lead on lead_eventos(lead_id, criado_em desc);
```

### Dependência
Depende de 6.1 (Kanban — a timeline normalmente é aberta a partir do card) e de todos os épicos que gravam `lead_eventos` (3.x, 4.x). Como esta story só **lê** eventos já gravados, não precisa esperar todos os produtores estarem prontos — pode ser construída assim que houver pelo menos um tipo de evento sendo gravado (3.1 em diante).

### Testing
Sem padrão dedicado — verificar que todos os `tipo` de evento definidos no enum/convenção de §17.4 têm representação visual, sem "tipo desconhecido" na UI.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-6-crm.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
