# Story 3.1: Função `identificar_lead` e migration (PRD: S-09)

## Status
Ready

## Executor Assignment
executor: "data-engineer"
quality_gate: "dev"
quality_gate_tools: ["concurrency-race-test"]

## Story
**Como** plataforma que nunca pode perder um lead,
**quero** a função `identificar_lead` atômica (uma função Postgres, não lógica de aplicação),
**para que** nenhuma mensagem exista sem um contato e um lead associados, mesmo sob concorrência (Princípio P5, Decisão A8).

## Acceptance Criteria
1. Contato e lead são criados **antes do debounce**, com id, telefone, data e status `novo`, e a timeline registra `lead_criado` (AC 9.1).
2. Mensagem que a IA não consegue interpretar ainda assim resulta em lead existente com status `novo` (AC 9.2).
3. Duas mensagens simultâneas do mesmo telefone novo resultam em um único lead — corrida tratada por `on conflict` em `contatos` e função `plpgsql` atômica (T9, §23.2).
4. Função implementa a matriz de decisão completa de §9.4: continuação (<7 dias), retomada (≥7 dias), reengajamento (`ganho`/`descartado`), e reversão de opt-out.
5. `janela_retomada_dias` é lida de `ia_config` por tenant (padrão 7, mín. 1, máx. 90) — não hardcoded.

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Database. Secondary: API. Complexity: M.
**Specialized Agents** — Primary: @data-engineer. Quality Gate: @dev.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [ ] Task 1 — Criar tabelas `contatos`, `leads`, `lead_interesses`, `lead_eventos` (§17.4) (AC: 1)
- [ ] Task 2 — Implementar `identificar_lead(p_tenant, p_telefone, p_push_name, p_origem)` literalmente conforme §17.10 (AC: 1, 2, 3, 4)
- [ ] Task 3 — RLS nas quatro tabelas novas, seguindo o padrão de 1.1 (AC: implícito, DoD)
- [ ] Task 4 — Teste de concorrência: duas chamadas simultâneas da função para o mesmo telefone novo (AC: 3, T9)
- [ ] Task 5 — Teste dos 7 cenários da matriz §9.4 contra banco real, não mock (§23.1: "identificar_lead | Integração (banco real)")

## Dev Notes

### Função (fonte literal: PRD §17.10 — implementar exatamente assim, é a especificação, não um exemplo)
```sql
create or replace function identificar_lead(
  p_tenant uuid, p_telefone text, p_push_name text, p_origem lead_origem
) returns table (contato_id uuid, lead_id uuid, entrada tipo_entrada)
language plpgsql as $$
declare
  v_contato uuid; v_lead uuid; v_status lead_status;
  v_ultimo timestamptz; v_seq int; v_janela int; v_entrada tipo_entrada;
begin
  select janela_retomada_dias into v_janela from ia_config where tenant_id = p_tenant;
  v_janela := coalesce(v_janela, 7);

  insert into contatos (tenant_id, telefone, nome)
  values (p_tenant, p_telefone, p_push_name)
  on conflict (tenant_id, telefone) do update
    set ultimo_contato_em = now(),
        nome = coalesce(contatos.nome, excluded.nome)
  returning id into v_contato;

  select id, status, ultimo_contato_em, lead_seq
    into v_lead, v_status, v_ultimo, v_seq
  from leads where contato_id = v_contato
  order by criado_em desc limit 1;

  if v_lead is null then
    v_entrada := 'primeiro_contato';
  elsif v_status in ('ganho','descartado') then
    v_entrada := 'reengajamento';
  elsif now() - v_ultimo >= (v_janela || ' days')::interval then
    v_entrada := 'retomada';
  else
    v_entrada := 'continuacao';
  end if;

  if v_entrada in ('primeiro_contato','reengajamento') then
    insert into leads (tenant_id, contato_id, lead_seq, origem, reaberto_de_lead_id)
    values (p_tenant, v_contato, coalesce(v_seq,0)+1, p_origem,
            case when v_entrada='reengajamento' then v_lead else null end)
    returning id into v_lead;

    update contatos set total_leads = total_leads + 1 where id = v_contato;

    insert into lead_eventos (tenant_id, lead_id, tipo, para, autor, detalhe)
    values (p_tenant, v_lead, 'lead_criado', 'novo', 'sistema',
            jsonb_build_object('entrada', v_entrada));
  else
    update leads
      set ultimo_contato_em = now(),
          reaberto_em = case when v_entrada='retomada' then now() else reaberto_em end
      where id = v_lead;

    if v_entrada = 'retomada' then
      insert into lead_eventos (tenant_id, lead_id, tipo, autor, detalhe)
      values (p_tenant, v_lead, 'reaberto', 'sistema',
              jsonb_build_object('dias_silencio', extract(day from now() - v_ultimo)));
    end if;
  end if;

  return query select v_contato, v_lead, v_entrada;
end $$;
```

Nota: a versão de §17.10 não trata explicitamente `opt_out=true` (linha da matriz §9.4: "Existe, opt_out=true → cria lead e reverte opt-out"). O @dev deve estender a função para reverter `contatos.opt_out` e gravar `opt_out_revertido_em` quando aplicável — reversão de opt-out também é AC 9.14 e AC 15.7, compartilhado com o Épico 7.

### Schema de suporte (fonte literal: PRD §17.4)
Ver `contatos`, `leads`, `lead_interesses`, `lead_eventos` completos em PRD-ALFAIA-v2.md §17.4.

### Matriz de decisão (fonte literal: PRD §9.4)
| Situação do contato | Situação do último lead | Silêncio | Ação | Status resultante |
|---|---|---|---|---|
| Não existe | — | — | Cria contato + lead | `novo` |
| Existe | Ativo | < 7 dias | Continuação — reusa o lead | Mantém o atual |
| Existe | Ativo | ≥ 7 dias | Retomada — reusa o lead, marca `reaberto_em` | Mantém, IA reavalia |
| Existe | `ganho` | qualquer | Reengajamento — novo lead | `novo` |
| Existe | `descartado` | qualquer | Reengajamento — novo lead | `novo` |
| Existe | Nenhum lead | — | Cria lead | `novo` |
| Existe, `opt_out=true` | qualquer | — | Cria lead e reverte opt-out | `novo` |

### Testing
§23.1: "`identificar_lead` | Integração (banco real) | Os 7 cenários da matriz §9.4". §23.2 lista T1, T2, T5, T6, T9 como casos obrigatórios para esta story especificamente.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-3-identificacao.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
