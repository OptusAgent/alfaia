# Story 7.3: Segmentação e prévia (PRD: S-29)

## Status
Done

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["segment-preview-accuracy-test"]

## Story
**Como** dona da loja,
**quero** ver quantos contatos e uma amostra antes de disparar uma campanha,
**para que** eu não dispare para o público errado por engano.

## Acceptance Criteria
1. Fluxo: definir segmento → prévia com contagem e amostra → escolher mensagem → agendar ou disparar (§15.2).
2. Campanha com segmento, ao pedir prévia, mostra contagem exata e amostra representativa (AC 15.2).
3. Prévia já exclui contatos em opt-out (usa a mesma regra de 7.2 — nunca mostra número inflado que inclui quem não pode receber).

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Frontend. Secondary: API. Complexity: M.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [x] Task 1 — Query de segmentação reaproveitando os filtros de 7.1 (evento, mês, papel, valor, categoria, status final, data, origem) (AC: 1, 2)
- [x] Task 2 — Tela de prévia com contagem exata + amostra (AC: 2)
- [x] Task 3 — Excluir opt-out da contagem/amostra (AC: 3)
- [x] Task 4 — Rota `POST /api/campanhas` criando o rascunho com o segmento (§18.2, tabela `campanhas`, §17.8)

## Dev Notes

### Schema (fonte literal: PRD §17.8)
```sql
create table campanhas (
  id uuid primary key default gen_random_uuid(),
  tenant_id uuid not null references tenants(id) on delete cascade,
  nome text not null,
  segmento jsonb not null,
  mensagem_id uuid references mensagens_followup(id),
  status text not null default 'rascunho',
  agendada_para timestamptz,
  total int not null default 0,
  enviados int not null default 0,
  falhas int not null default 0,
  respondidos int not null default 0,
  pausada_motivo text,
  criado_em timestamptz not null default now()
);
```

### Dependência
Depende de 7.1 (base/filtros) e 7.2 (opt-out, para não inflar a prévia).

### Testing
AC 15.2 é a fonte formal.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-7-campanhas.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
| 2026-08-09 | 1.2 | Implementação concluída da migration campanhas table, serviço CampanhaService, rotas POST /api/campanhas/previa e POST /api/campanhas, e Wizard de Nova Campanha no Portal — Status: Ready → InProgress → InReview | Dex (@dev) |
| 2026-08-09 | 1.3 | QA Gate Aprovado (verdict: PASS) — Status: InReview → Done | Quinn (@qa) |


