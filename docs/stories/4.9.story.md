# Story 4.9: Envio de imagem e detalhes reais do produto no WhatsApp

## Status
Done

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["media-send-test", "no-invented-media-test"]

## Story
**Como** lead conversando pelo WhatsApp,
**quero** ver a foto real da peça que a IA está me oferecendo, junto com cor, tamanho e valor,
**para que** eu decida se gosto do modelo sem precisar perguntar de novo ou ir até a loja (P3, princípio "dado real").

## Contexto — por que esta story existe
Achado real em produção (conversa via WhatsApp sandbox, 2026-08-20/21, mesma sessão que gerou as stories 4.8 e os hotfixes de 3.3/5.3/5.4): depois do tool-calling real funcionar (4.8) e do agendamento gravar dados corretos (5.3), o lead relatou que a IA "reconhece [o pedido] mas não mostra as imagens com os detalhes de tamanho e cor". Investigação confirmou:

- `worker/app/routers/webhooks.py:251` só chama `adapter.enviar_texto(...)` — **nunca** chama `adapter.enviar_midia(...)`, que já existe, já funciona e já foi validado (story 2.3, `Done`; `UAZAPIAdapter.enviar_midia` faz `POST /send/media` de verdade).
- O resultado real da tool `buscar_produtos` (`product_search_service` → `weblocacao_service`) já traz `imagem`, `cor`, `tamanho` e `valor_aluguel` por produto — o dado existe e é real, só não é usado no envio nem citado em detalhe no texto da IA.
- A tabela `mensagens` já tem colunas `midia_url`/`midia_tipo` prontas no schema — nunca usadas até hoje.
- Não existe nenhuma menção a envio de imagem/mídia de produto em nenhum PRD (`docs/prd/epic-4-atendimento.md`, `epic-5-integracao-wl.md`) nem em nenhuma story anterior (4.3, 5.2, 5.3). **Não é regressão de nenhuma story `Done` — é capacidade nova, nunca especificada.**

## Acceptance Criteria
1. Quando `buscar_produtos` é executada com sucesso dentro do loop de tool-calling (story 4.8) e retorna 1+ produtos, o motor de IA (`ai_engine.py`) monta uma lista de mídias reais a enviar — no máximo 3 produtos por troca (evitar flood no WhatsApp) — cada uma com `url` (campo `imagem` do produto) e `legenda` contendo nome, cor, tamanho(s) disponíveis e valor de locação, **todos vindos do resultado real da tool, nunca inventados** (P3).
2. `AIResponseDTO` ganha um campo novo `midias_sugeridas: list[MidiaEnvioDTO]` (pode ser vazio) — não quebra nenhum chamador existente (default `[]`).
3. `webhooks.py`, ao processar a resposta da IA, envia cada mídia sugerida via `adapter.enviar_midia(to=telefone, url=..., tipo="image", caption=...)` **antes** de enviar o texto final (ordem natural: primeiro as fotos, depois a pergunta/confirmação em texto).
4. Cada mídia enviada com sucesso é registrada em `mensagens` preenchendo `midia_url` e `midia_tipo` (colunas já existentes no schema), com `texto`/`conteudo` igual à legenda, mantendo o histórico real da conversa consistente com o que o lead viu.
5. Falha ao enviar uma mídia específica (erro do adapter/rede) não bloqueia as demais mídias nem o envio do texto final — é uma falha de canal/entrega, não de dado (nunca aciona `abrir_transbordo` por conta disso), mas é logada como warning.
6. O texto final da IA, quando cita um produto (com ou sem imagem enviada com sucesso), sempre menciona nome, cor, tamanho e valor reais retornados pela tool — nunca uma frase vaga tipo "encontrei algumas opções" sem detalhe (ajuste de instrução em `TOOL_DESCRIPTIONS`/prompt).
7. Nenhuma mídia é enviada a partir de dado que não veio de uma execução real de `buscar_produtos` nesta troca (nunca reaproveitar imagem de turno anterior nem inventar URL) — mesmo princípio P3 já aplicado ao texto pela 4.3/4.8.
8. Teste cobre: produto com imagem enviado corretamente (mock do adapter), limite de 3 mídias respeitado com mais de 3 produtos retornados, falha de uma mídia não impede as demais nem o texto, e nenhuma mídia é sugerida quando `buscar_produtos` não foi chamada ou falhou.

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Atendimento/Canal. Secondary: Integração WL. Complexity: M.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Enviar mídia/legenda com dado não vindo de tool real seria CRITICAL (quebra P3, mesmo padrão já tratado em 4.3/4.8). Falha de envio de mídia bloquear a troca inteira (texto incluído) seria HIGH (degradação evitável — a mídia é enriquecimento, não requisito de resposta). Limite de mídias por troca ausente seria MEDIUM (risco de spam/custo, não de dado).

## Tasks / Subtasks
- [x] Task 1 — Criar `MidiaEnvioDTO` (Pydantic: `url: str`, `legenda: str`) em `ai_engine.py` ou `tools_registry.py`; `AIResponseDTO` ganha `midias_sugeridas: list[MidiaEnvioDTO] = []` (AC: 2)
- [x] Task 2 — Em `_processar_tool_calling_loop`/`processar_atendimento`, capturar o resultado real de `buscar_produtos` quando executada com sucesso nesta troca e montar até 3 `MidiaEnvioDTO` (produto com `imagem` presente, ranqueados igual ao retorno da tool — já vem ordenado por disponibilidade) (AC: 1, 7)
- [x] Task 3 — `webhooks.py`: antes do envio de texto, iterar `res_ia.midias_sugeridas` chamando `adapter.enviar_midia(to=telefone, url=m.url, tipo="image", caption=m.legenda)`; capturar falha individual sem interromper o restante (AC: 3, 5)
- [x] Task 4 — Registrar cada mídia enviada com sucesso via `supabase_rest_service.registrar_mensagem(...)` preenchendo `midia_url`/`midia_tipo`/`texto` (AC: 4)
- [x] Task 5 — Ajustar `TOOL_DESCRIPTIONS["buscar_produtos"]` e/ou instrução do prompt para a IA sempre citar nome, cor, tamanho e valor reais ao apresentar um produto no texto (AC: 6)
- [x] Task 6 — Testes: envio de mídia com sucesso, limite de 3 respeitado, falha parcial não bloqueia texto nem demais mídias, nenhuma mídia sugerida sem `buscar_produtos` bem-sucedida na troca atual (AC: 8)

## Dev Notes

### O que já existe e não muda
- `UAZAPIAdapter.enviar_midia` (`worker/app/adapters/uazapi.py:401`) — já implementado e validado (story 2.3, `Done`). Não recriar, só chamar.
- `product_search_service.buscar_produtos_com_cache` já retorna `imagem`, `cor`, `tamanho`, `valor_aluguel`, `nome` reais por produto (via `WLProdutoDTO`) — não precisa de nova chamada à WebLocação nem de novo campo no contrato.
- Colunas `midia_url`/`midia_tipo` já existem em `mensagens` — não precisa de migration.

### Decisão de escopo (assumida nesta story, sem especificação prévia no PRD)
Limite de 3 imagens por troca é uma escolha de implementação, não uma regra de produto documentada — @architect pode ajustar no gate se achar pouco/muito, mesmo padrão de `MAX_TOOL_CALL_ITERATIONS` na 4.8. Ordem escolhida (imagens antes do texto) segue o padrão natural de catálogo em WhatsApp (mostrar antes de perguntar).

### Fora de escopo desta story
- Álbum/carrossel de mídia (várias imagens numa única mensagem) — UAZAPI/WhatsApp tratam como mensagens sequenciais; não inventar suporte a carrossel sem confirmar no contrato do provedor.
- Envio de imagem do comprovante de agendamento ou de outros tipos de mídia (áudio, documento) — fora do gap relatado.
- Persistir preferência de "já viu estas fotos" entre trocas — cada troca decide de novo com base no resultado real da tool, mesmo princípio da 4.8 (não inventar mecanismo de estado não pedido).

### Referências
- `worker/app/routers/webhooks.py:251` (ponto exato do gap)
- `worker/app/adapters/uazapi.py:401` (`enviar_midia`, já pronto)
- `worker/app/services/product_search_service.py`, `worker/app/services/weblocacao_service.py` (`WLProdutoDTO.imagem/cor/tamanho`)
- `worker/app/services/ai_engine.py` (`_processar_tool_calling_loop`, story 4.8)
- Story 4.8 (loop de tool-calling real, pré-requisito funcional), Story 2.3 (`enviar_midia`, `Done`)

### Testing
Mock do adapter (`enviar_midia`) para os 4 cenários da AC 8, seguindo o mesmo padrão de dublês (`FakeLLM`/doubles) já usado em `test_ai_engine.py` e `test_webhooks.py`.

## Dev Agent Record

Implementação completa em uma sessão (Tasks 1-6). `product_search_service`/mock de `weblocacao_service`
em `WL_MODO=mock` só tem 2 produtos sintéticos cadastrados (catálogo pequeno de desenvolvimento,
diferente do catálogo do `wl-fake-api` externo usado para as imagens do bucket Supabase) — o teste
de limite de 3 mídias (`test_motor_respeita_limite_de_midias_por_troca`) precisou de um resultado
sintético de `buscar_produtos` (monkeypatch pontual de `tools_registry.executar_tool`) para exercitar
5 produtos, já que o mock real nunca devolve mais de 2. Os demais testes usam o registro real.

165/165 testes verdes (161 pré-existentes + 4 novos: mídia montada corretamente, limite de 3
respeitado, nenhuma mídia sem busca real na troca, envio+falha-parcial não bloqueia texto).

## Architect Gate (quality_gate: architect)

**Veredito: PASS**

Revi os 3 pontos de risco listados no CodeRabbit Integration desta story:
1. **Dado não vindo de tool real** — `midias_sugeridas` só é preenchida dentro do bloco que já
   executou `tools_registry.executar_tool("buscar_produtos", ...)` com sucesso nesta troca; é
   zerada quando `transbordo_acionado` (nunca envia mídia de uma oferta que não se confirmou).
   Confirmado por leitura direta e pelos testes `test_motor_monta_midias_reais_...` e
   `test_motor_nao_sugere_midia_sem_buscar_produtos_na_troca`.
2. **Falha de mídia bloquear a troca** — `webhooks.py` usa `try/except` + checagem de
   `res_midia.sucesso` por item, com `continue` em ambos os casos; o envio do texto final não
   depende do resultado das mídias. Confirmado pelo teste de falha parcial.
3. **Limite de mídias** — corrigido durante a revisão: o corte por `MAX_MIDIAS_POR_TROCA` agora é
   aplicado *depois* de filtrar por `imagem` presente (antes cortava primeiro e filtrava depois,
   podendo devolver menos de 3 mídias mesmo havendo produtos com imagem além do índice 3). Ajuste
   pequeno, coberto pelo teste de limite existente.

Ponto observado, não bloqueante: o catálogo mock em `WL_MODO=mock` (`weblocacao_service`, usado
pelos testes) só tem 2 produtos sintéticos — diferente do catálogo do `wl-fake-api` externo (9
produtos, usado nas imagens reais do bucket Supabase). Isso é preexistente à story (não introduzido
aqui) e já está documentado no Dev Agent Record; só registra para não confundir os dois catálogos
mock em auditorias futuras.

165/165 testes verdes. Sem débito novo registrado.

### File List
- `worker/app/services/ai_engine.py` — `MidiaEnvioDTO`, `AIResponseDTO.midias_sugeridas`, `MAX_MIDIAS_POR_TROCA`, `_processar_tool_calling_loop` captura mídias reais de `buscar_produtos` (substitui, não acumula, entre buscas na mesma troca)
- `worker/app/services/tools_registry.py` — `TOOL_DESCRIPTIONS["buscar_produtos"]` instrui a IA a detalhar nome/cor/tamanho/valor no texto e não afirmar que está "enviando imagem" (o sistema já envia)
- `worker/app/routers/webhooks.py` — envia `res_ia.midias_sugeridas` via `adapter.enviar_midia` antes do texto final; falha individual não bloqueia; registra cada mídia enviada em `mensagens` (`midia_url`/`midia_tipo`)
- `worker/tests/test_ai_engine.py` — 3 testes novos (mídia montada, limite de 3, nenhuma mídia sem busca)
- `worker/tests/test_webhook_uazapi_integration.py` — stub `FakeAI` atualizado com `midias_sugeridas=[]`; 1 teste novo (envio de mídia + falha parcial não bloqueia texto)

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-21 | 1.0 | Story criada a partir de achado real em produção (conversa via WhatsApp, mesma sessão da 4.8/hotfixes 3.3-5.3-5.4): IA reconhece o produto mas nunca envia imagem nem detalha cor/tamanho/valor no texto. Sem ID `S-` do PRD original — capacidade nova, nunca especificada (mesmo padrão emergente da 4.8) | River (@sm) |
| 2026-08-21 | 1.1 | Validado GO (10/10) — dependências (2.3 `enviar_midia`, 4.8 loop de tool-calling) já `Done`, nenhuma credencial externa pendente (roda sobre o mesmo catálogo mock já usado por 4.3/5.2), escopo e limites (3 mídias/troca) explicitados como decisão de implementação sujeita a ajuste do @architect. Status: Draft → Ready | Pax (@po) |
| 2026-08-21 | 1.2 | Implementadas Tasks 1-6: `MidiaEnvioDTO`/`midias_sugeridas` no motor de IA, envio real via `enviar_midia` antes do texto em `webhooks.py`, registro em `mensagens` (`midia_url`/`midia_tipo`), instrução de prompt para detalhar produto em texto. 165/165 testes verdes. Status: Ready → InProgress → InReview | Dex (@dev) |
| 2026-08-21 | 1.3 | Architect Gate: PASS. Corrigido corte de limite de mídias para filtrar por imagem antes de aplicar `MAX_MIDIAS_POR_TROCA` (evita devolver menos de 3 mídias indevidamente). 165/165 testes verdes. Status: InReview → Done | Aria (@architect) |
