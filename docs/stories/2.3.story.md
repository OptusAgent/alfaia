# Story 2.3: Adapter UAZAPI — envio + webhook (PRD: S-05)

## Status
Ready

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["adapter-contract-test", "header-consistency-check"]

## Story
**Como** tenant que ainda não fechou parceria oficial Meta/AuctaFlux,
**quero** um adapter UAZAPI funcional (canal não oficial, WhatsApp Web),
**para que** o produto opere desde o primeiro tenant, sem depender de aprovação Meta ou credenciais Aucta.

## Acceptance Criteria
1. Implementa `CanalAdapter` (de 2.1) para UAZAPI: `normalizar_webhook` a partir de `POST /webhook/{token}`; `enviar_texto` via `POST /send/text`; `enviar_midia` via `POST /message/sendMedia/{instance}` (AC 14.1).
2. Encapsula a inconsistência de headers documentada no PRD: `token` em `/send/text`, `apikey` em `/message/sendMedia/{instance}` — **nenhuma camada acima do adapter sabe dessa diferença** (nota de implementação, §14.1).
3. `capabilities` retorna `janela_24h=False`, `requer_delay=True` com `delay_min_ms`/`delay_max_ms` configurados para anti-ban, `exige_horario_comercial=False` (regra geral do canal — horário comercial é regra de campanha, K1, não do adapter em si).
4. Payload normalizado idêntico estruturalmente ao contrato de §6.3, reaproveitando a captura de webhook e idempotência de 2.2.
5. Presence `composing` enviado antes da resposta, com delay configurável, para reduzir risco de banimento.
6. Suporte às chamadas de gestão de instância da UAZAPI: criação administrativa via `POST /instance/create` (`name`, `adminField01`), geração de sessão/QR Code via `POST /instance/connect` (`browser`, `systemName`, `proxy_managed_*`) e associação automática da URL do webhook (`POST /webhook/{instance_name}`).

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Integration. Secondary: Security. Complexity: M.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [ ] Task 1 — Implementar `enviar_texto`/`enviar_midia` com o roteamento de header correto por endpoint (AC: 1, 2)
- [ ] Task 2 — Implementar `normalizar_webhook` a partir do payload UAZAPI real (AC: 1, 4)
- [ ] Task 3 — Implementar `capabilities` do provider UAZAPI (AC: 3)
- [ ] Task 4 — Implementar delay + `presence: composing` (AC: 5)
- [ ] Task 5 — Registrar credenciais (`uazapi_base_url`, `uazapi_instancia`, `uazapi_token`) na tabela `canais` (§17.3), nunca em código (I1/S2, §7.3/§22)
- [ ] Task 6 — Implementar cliente HTTP no `worker` para criação de instância (`POST /instance/create`) e inicialização de sessão de QR Code (`POST /instance/connect`) (AC: 6)
- [ ] Task 7 — Implementar registro automático da URL de Webhook no UAZAPI (`POST /webhook/{instance_name}`) (AC: 6)

## Dev Notes

### Disponibilidade de credenciais e Endpoints UAZAPI (Doc oficial 2.1.0/2.1.1)
O usuário confirmou nesta sessão que **já tem acesso à UAZAPI** — esta story não tem bloqueio externo.
- **Criar Instância**: `POST /instance/create` com `{ "name": "...", "adminField01": "tenant_id" }`
- **Iniciar Pareamento / Obter QR Code**: `POST /instance/connect` com `{ "browser": "Chrome", "systemName": "ALFAIA Portal", "proxy_managed_country": "BR" }`
- **Configurar Webhook na UAZAPI**: `POST /webhook/{instance_name}`

### Schema (fonte literal: PRD §17.3)
```sql
create table canais (
  id uuid primary key default gen_random_uuid(),
  tenant_id uuid not null references tenants(id) on delete cascade,
  provider canal_provider not null,
  nome text not null,
  ativo boolean not null default false,
  uazapi_base_url text,
  uazapi_instancia text,
  uazapi_token text,
  status text default 'desconectado',
  qualidade text,
  aquecimento_iniciado_em date,
  criado_em timestamptz not null default now()
);
create unique index canais_um_ativo on canais(tenant_id) where ativo;
```

### Comparativo (fonte literal: PRD §14.1)
| | UAZAPI |
|---|---|
| Tipo | Não oficial (WhatsApp Web) |
| Auth | Token por instância |
| Recebimento | `POST /webhook/{token}` |
| Envio texto | `POST /send/text`, header `token` |
| Envio mídia | `POST /send/media` |
| Instância / QR | `POST /instance/create` & `POST /instance/connect` |
| Janela 24h | Não se aplica |
| Anti-ban | `delay` + `presence: composing` |

### Testing
Padrão §23.1: "Adapters de canal | Unitário | Normalização, HMAC, idempotência, headers — 100% dos caminhos".

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-2-canal.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
| 2026-08-09 | 1.2 | Atualizado com endpoints de Gestão de Instância e QR Code UAZAPI (v2.1.0) | River (@sm) |

