# Story 6.5: Disparo manual de follow-up (PRD: S-26)

## Status
Ready

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["meta-window-gate-test"]

## Story
**Como** dona da loja,
**quero** escolher a mensagem de follow-up e disparar com um clique,
**para que** eu decida o tom, mas não precise escrever do zero toda vez.

## Acceptance Criteria
1. Lojista dispara mensagem escolhida → enviada pelo canal ativo e registrada na timeline (AC 10.2).
2. Canal Meta com janela fechada → modal só permite mensagens com `template_meta` correspondente (AC 10.6, §10.2).
3. Ação restrita a `dono` e `atendente` (§4.2: "Disparar follow-up" — `operador` = ❌).
4. Rota `POST /api/leads/{id}/followup` (§18.2).

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Frontend. Secondary: Integration. Complexity: M.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [ ] Task 1 — Modal de seleção de mensagem (`mensagens_followup`, §17.8) (AC: 1, 2)
- [ ] Task 2 — Filtrar mensagens elegíveis por `template_meta` quando o canal ativo é Meta com janela fechada (AC: 2) — reaproveita `capabilities` de 2.1/2.5
- [ ] Task 3 — Guard de permissão `has_permission('followup','create')` restrito a `dono`/`atendente` (AC: 3)
- [ ] Task 4 — Rota `/api/leads/{id}/followup` disparando pelo adapter do canal ativo (AC: 4)

## Dev Notes

### Schema (fonte literal: PRD §17.8)
```sql
create table mensagens_followup (
  id uuid primary key default gen_random_uuid(),
  tenant_id uuid not null references tenants(id) on delete cascade,
  nome text not null,
  corpo text not null,
  template_meta text,
  ativa boolean not null default true
);
```

### Dependência
Depende de 6.4 (worker que move para `follow_up` e habilita o botão) e de 2.5 (seletor de canal/capabilities, para o gate de janela de 24h).

### Testing
AC 10.2 e AC 10.6 são a fonte formal.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-6-crm.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
