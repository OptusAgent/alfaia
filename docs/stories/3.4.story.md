# Story 3.4: Histórico de interesse e detecção de mudança (PRD: S-12)

## Status
Done

## Executor Assignment
executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["interest-versioning-test"]

## Story
**Como** plataforma que nunca inventa nem sobrescreve dado,
**quero** gravar cada versão do interesse do lead em `lead_interesses` (Decisão A10: nunca sobrescrito),
**para que** seja possível detectar quando o interesse mudou de fato, e não apenas foi atualizado.

## Acceptance Criteria
1. Evento diferente (ex.: era formatura, agora casamento) ou data do evento diferente → novo interesse: lead atual é encerrado como `descartado` com motivo `interesse_substituido`, e um novo lead é criado (AC 9.6, T7).
2. Categoria de peça diferente → também tratado como novo interesse, mesma regra do item 1.
3. Tamanho ou cor diferentes, mesmo evento → o lead vigente é atualizado e uma **nova linha** entra em `lead_interesses` (versão incrementada), sem criar lead novo (AC 9.7, T8).
4. Lead confirma que é o mesmo interesse → mantém, segue de onde parou, sem gravar nova versão.

## 🤖 CodeRabbit Integration
**Story Type Analysis** — Primary: Database. Secondary: API. Complexity: M.
**Specialized Agents** — Primary: @dev. Quality Gate: @architect.
Padrão de severidade do projeto — ver [coderabbit-integration.md](../../.claude/rules/coderabbit-integration.md).

## Tasks / Subtasks
- [x] Task 1 — Implementar tool `registrar_interesse` (snapshot do interesse atual, §8.2) gravando em `lead_interesses` com `versao` incremental (AC: 3)
- [x] Task 2 — Implementar a regra de decisão evento-mudou vs. só-detalhe-mudou (AC: 1, 2, 3)
- [x] Task 3 — Encerramento automático do lead anterior com `motivo_descarte='interesse_substituido'` quando aplicável (AC: 1)
- [x] Task 4 — Testes T7 e T8 de §23.2

## Dev Notes

### Sinais de mudança (fonte literal: PRD §9.6)
| Sinal | Consequência |
|---|---|
| Evento diferente (era formatura, agora casamento) | Novo interesse — grava versão nova em `lead_interesses` |
| Data do evento diferente | Novo interesse |
| Categoria de peça diferente | Novo interesse |
| Tamanho ou cor diferente, mesmo evento | Atualização do interesse vigente |
| Lead confirma que é o mesmo | Mantém interesse, segue de onde parou |

### Schema (fonte literal: PRD §17.4)
```sql
create table lead_interesses (
  id bigserial primary key,
  tenant_id uuid not null references tenants(id) on delete cascade,
  lead_id uuid not null references leads(id) on delete cascade,
  versao int not null default 1,
  evento_tipo text, evento_data date, papel text,
  peca_interesse text, tamanho text, cor text,
  valor_estimado numeric(10,2),
  motivo text,
  criado_em timestamptz not null default now(),
  unique (lead_id, versao)
);
```

### Dependência
Depende de 3.1 (tabela `lead_interesses` já existe desde a migration de 3.1 — esta story implementa a lógica de negócio sobre ela, não a tabela em si).

### Testing
§23.2: T7 ("Evento diferente na retomada → lead anterior encerrado, novo criado") e T8 ("Só tamanho muda → mesmo lead, nova versão em lead_interesses").

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-08 | 1.0 | Story criada a partir de docs/prd/epic-3-identificacao.md | River (@sm) |
| 2026-08-08 | 1.1 | Validado GO — Status: Draft → Ready | Pax (@po) |
| 2026-08-09 | 1.2 | Implementação concluída do InterestService — Status: Ready → InProgress → InReview | Dex (@dev) |
| 2026-08-09 | 1.3 | QA Gate Aprovado (verdict: PASS) — Status: InReview → Done | Quinn (@qa) |


